<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jerrygcding.github.io//feed.xml" rel="self" type="application/atom+xml"/><link href="https://jerrygcding.github.io//" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-10T04:21:53+00:00</updated><id>https://jerrygcding.github.io//feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">The Underrated Decoder Behind a Unified Correspondence Model</title><link href="https://jerrygcding.github.io//blog/2026/underrated-match-dec/" rel="alternate" type="text/html" title="The Underrated Decoder Behind a Unified Correspondence Model"/><published>2026-05-04T10:00:00+00:00</published><updated>2026-05-04T10:00:00+00:00</updated><id>https://jerrygcding.github.io//blog/2026/underrated-match-dec</id><content type="html" xml:base="https://jerrygcding.github.io//blog/2026/underrated-match-dec/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>3D computer vision is in the middle of a quiet consolidation. Tasks that used to live in their own walled gardens — depth estimation, structure from motion, point cloud registration — are increasingly being absorbed into a small number of large, Transformer-based foundation models like <a href="https://arxiv.org/abs/2312.14132">DUSt3R</a>, <a href="https://arxiv.org/abs/2406.09756">MASt3R</a>, and <a href="https://arxiv.org/abs/2503.11651">VGGT</a>. The pattern is the same one that played out in NLP a few years earlier: a single architecture, scaled with diverse data, ends up generalizing better than a zoo of specialists.</p> <p>There is one task sitting at the very foundation of 3D vision that has stubbornly resisted this consolidation: <strong>visual correspondence</strong>. The job of finding the same physical point across two different observations is the substrate that registration, camera pose estimation, SLAM, and SfM all stand on. And it shows up in three flavors — image-to-image (2D-2D), image-to-point cloud (2D-3D), and point cloud-to-point cloud (3D-3D) — depending on what modality the source and target observations come in. Despite all three being the same problem at heart, <em>given a keypoint here, find its match over there</em>, the field still trains a separate specialist for each.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/geo-matching/unicorrn_teaser-480.webp 480w,/assets/img/blog/geo-matching/unicorrn_teaser-800.webp 800w,/assets/img/blog/geo-matching/unicorrn_teaser-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/geo-matching/unicorrn_teaser.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> UniCorrn finds correspondences across 2D-2D, 2D-3D, and 3D-3D pairs with a single set of weights. </div> <p>In our recent work <a href="https://arxiv.org/abs/2605.04044"><em>UniCorrn</em></a>, we asked whether one model can do all three. The short answer is yes — a single 600M-parameter model with shared weights matches the leading 2D-2D specialists and beats the leading 2D-3D and 3D-3D specialists by 8% and 10% in registration recall on 7Scenes and 3DLoMatch, respectively.</p> <p>The longer answer, which is what I want to write about here, is that getting there required committing to a <strong>matching decoder</strong> design that I think is badly under-explored relative to its potential. This post is about that decoder — the design choices that make it work, why the matching decoder is the underrated piece in current correspondence foundation models, and why I think this kind of design is the right substrate to bet on for a future Large Correspondence Model.</p> <p>I want to walk through the reasoning roughly the way I worked through it, because each design move only really makes sense in light of the constraint the previous one created.</p> <p>Underneath the technical specifics is a research instinct I keep returning to: that the most interesting wins in modern computer vision sit at the intersection of classical structural priors and large neural architectures, and that scaling carries you furthest when it is paired with the right explicit inductive bias rather than asked to do everything alone. UniCorrn is one such pairing. This post is about how it was built.</p> <h2 id="previous-works-in-visual-correspondence">Previous Works in Visual Correspondence</h2> <p>Before getting to the design space I want to defend, it is worth laying out what people have been doing instead. Existing approaches to learning visual correspondence — including the ones that have tried to unify across multiple settings — fall into roughly four families.</p> <p><strong>Cost volume and dense warp refinement.</strong> <a href="https://arxiv.org/abs/2003.12039">RAFT</a> is the canonical case for optical flow: build a 4D correlation volume between feature maps and refine the flow estimate iteratively through a recurrent unit. For wide-baseline matching, <a href="https://arxiv.org/abs/2202.00667">DKM</a> recasts matching as kernel regression and refines a dense warp through stacked feature maps; <a href="https://arxiv.org/abs/2305.15404">RoMa</a> extends this lineage with DINOv2 features, an anchor-probability match decoder, and ConvNet refiners at multiple resolutions. These methods currently top the 2D-2D leaderboards. The architectural cost of being accurate is rigidity: the pyramid depth is fixed, the recurrence has finite unrolling, and the underlying machinery — correlation volumes, warp grids — assumes a 2D pixel grid that does not extend gracefully to irregular 3D point sets.</p> <p><strong>Dense descriptor matching with a single-stage matching layer.</strong> <a href="https://arxiv.org/abs/2104.00680">LoFTR</a> learns dense descriptors with a Transformer and matches them through a dual-softmax mutual-matching layer at coarse resolution, refined locally at the fine level. <a href="https://arxiv.org/abs/2406.09756">MASt3R</a> augments DUSt3R with a dense feature head and uses fast reciprocal nearest-neighbor matching at inference. The descriptor and the matcher are tightly coupled, but the matching itself is a single stage at the end of a feature backbone — you cannot stack matching layers and refine through them the way Transformer decoders are designed to work, which means the descriptor has to do all the heavy lifting in a single forward pass.</p> <p><strong>Direct regression.</strong> <a href="https://arxiv.org/abs/2506.09278">UFM</a> is the cleanest current example: fuse the two image features with a generic Transformer encoder and directly regress the dense pixel displacement field. Fast, and it scales beautifully on 2D-2D. But our experiments show it falls over on 2D-3D and 3D-3D — without an explicit similarity mechanism in the architecture, the model has nowhere to encode geometric reasoning about <em>which target token actually corresponds to my source query</em>.</p> <p><strong>Sequence concatenation.</strong> <a href="https://arxiv.org/abs/2103.14167">COTR</a> concatenates source and target into a single sequence and lets a vanilla Transformer figure out matching. Flexible, but in our ablations (Table 1 of the paper) it performs the worst across all three settings, presumably because the model has to discover the matching structure from scratch with no built-in bias.</p> <p>What I want to argue is that the four families above all share a quiet assumption: the <em>matching decoder</em> is a small, low-investment afterthought sitting on top of a large feature backbone - nearest neighbor is one line; regression heads are an MLP; cost volumes do their work in a fixed correlation block before the network really starts thinking.</p> <p>The actually interesting design space sits in a matching decoder that hits all three desiderata at once — (1) end-to-end learning through stackable layers, (2) handling of irregular structures like point clouds without requiring a 2D grid, and (3) iterative geometric refinement of the correspondence estimate — and that space has been under-explored. The point of this post is what such a decoder looks like, and why I think building it right is the most consequential architectural choice in a unified correspondence model.</p> <h2 id="attention-and-matching">Attention and Matching</h2> <p>The starting observation is one that has been hiding in plain sight inside every Transformer ever written.</p> <p>Take two sets of tokens, source \(F_s\) from one observation and target \(F_t\) from another, and write down a single cross-attention layer. Compute the (row-normalized) attention matrix \(A\) between source queries and target keys:</p> \[A = \mathrm{Softmax}\left(\frac{F_s W_Q (F_t W_K)^\top}{\sqrt{D}}\right).\] <p>In an ideal world where features are perfectly discriminative, each row of \(A\) is a one-hot vector — and the position of the 1 in row \(i\) is exactly the index of the target token that matches source token \(i\). <em>That is a matching cost.</em> Up to row-normalization, \(A\) is the same object as a learnable cost volume (see <a href="https://arxiv.org/abs/2007.11431">Xiao et al. 2020</a>).</p> <p>Now do something slightly unusual with the value vector \(V\). Instead of feeding it the appearance features of the target tokens, feed it the <em>absolute positional encoding of every target token</em>. The output of the attention layer is then</p> \[Q = AV,\] <p>which, for each source query, returns the positional encoding of its corresponding target location. Push that through a linear layer and you have <em>coordinates</em>. The supplementary material of our paper has a tiny toy illustration of this with four pixels and four animal symbols that I think is genuinely the most useful one-figure summary of why the rest of the model looks the way it does.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/geo-matching/two_stream_idea-480.webp 480w,/assets/img/blog/geo-matching/two_stream_idea-800.webp 800w,/assets/img/blog/geo-matching/two_stream_idea-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/geo-matching/two_stream_idea.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Attention as a learnable matching cost. With perfect features, the attention matrix becomes a permutation matrix; multiplied by target positional encodings, it reads off the matching coordinates directly. </div> <p>This is the seed of what I think of as <strong>geometry-based matching with attention</strong>. It treats the attention matrix as the matching cost (geometric), and it produces matching <em>coordinates</em> (also geometric) rather than just matching descriptors. It is also automatically modality-agnostic: nothing about the construction assumes the target tokens come from a 2D grid, they could just as easily be 3D points.</p> <p>But there is an immediate problem. <strong>What happens when you try to stack two of these layers on top of each other?</strong></p> <h2 id="dual-stream-cross-attention">Dual-Stream Cross Attention</h2> <p>The reason single-layer cost-volume reasoning is not enough is that visual correspondence is hard. With real features, \(A\) is never a perfect permutation matrix; it is a soft, noisy approximation. The whole point of Transformer-style stacking is that we want layer 2 to look at the rough estimate from layer 1 and refine it. That requires layer 2 to compute its <em>own</em> attention matrix between source and target — which means it needs appearance features for the source query.</p> <p>And there is the rub: if layer 1 sets \(V\) to positional encoding to read off coordinates cleanly, the output is positional only — there are no appearance features for layer 2 to query with, and the design is fundamentally non-stackable. The alternative, keeping \(V\) as appearance features (the standard recipe used in basic regression-style decoders), does stack, but at the cost of blending: the prediction head now has to disentangle matching coordinates from a representation where positional and appearance signals are mixed together in the shared channels. The single-stream design forces a choice between these two failure modes.</p> <p>Getting there took a couple of detours. The earliest small-scale experiments — regression-style and sequence-concatenation baselines — failed by large margins, working only at large scale and only on 2D-2D. My first structural fix was to store positional features in a single hidden state and update them through a gated operator at each layer. That ran into exactly the failure mode above: positional features kept colliding with the appearance content the query was carrying, and stacking layers did not help. The disentangled, two-stream version suggested itself when I went back to the basics of how attention is supposed to work — queries and values are different things, and there is no rule against giving them their own residual streams when the values you want to read out are categorically different from the features you want to attend with.</p> <p>This is the gap the <strong>dual-stream Transformer decoder</strong> fills — the central design move in the paper, and the one I want to walk through carefully, because it is the move the rest of the decoder is designed around.</p> <p>The idea is to maintain <em>two parallel residual streams</em> through the decoder — one for appearance, one for position — and have them share a single attention matrix. Concretely, for each source keypoint we carry along both an appearance feature \(F_k\) and a positional embedding \(P_k\), the latter initialized to zero since we do not yet know where the match is. At every layer:</p> <ol> <li>Compute the attention matrix \(A\) from \(F_k\) and \(F_t\), with both streams contributing position information through RoPE.</li> <li>Update the appearance stream: \(F_k \leftarrow A \cdot W_V F_t + F_k\). This is the standard Transformer update.</li> <li>Update the positional stream: \(P_k \leftarrow A \cdot \mathrm{AbsPE}(X_t) + P_k\). The same attention matrix, but applied to the <em>target tokens’ absolute positional encoding</em>.</li> </ol> <p>Each stream has its own residual connection, so both quantities flow through the stack and are gradually refined. Crucially, the next layer recomputes \(A\) from the updated \(F_k\), so the attention itself improves layer over layer; and because \(P_k\) accumulates “weighted target positions” along the way, by the final layer it encodes the regressed coordinate of the corresponding keypoint. A single linear head pulls the coordinates out at the end via the Moore–Penrose inverse of the AbsPE map.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/geo-matching/dual_stream_decoder-480.webp 480w,/assets/img/blog/geo-matching/dual_stream_decoder-800.webp 800w,/assets/img/blog/geo-matching/dual_stream_decoder-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/geo-matching/dual_stream_decoder.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Dual-stream attention with a single shared attention matrix. Appearance features and positional embeddings travel in parallel residual streams; after attention, the joint output is split and each stream is updated independently. </div> <p>A few practical details I want to highlight, because each was a step on the path between the prototype and what is in the paper now.</p> <p><strong>Positional encoding: from sinusoidal to RoPE.</strong> In my initial prototype of the dual-stream layer I used additive sinusoidal positional encoding, applied directly to \(Q\) and \(K\) just before the attention computation rather than baked into the input embeddings (the vanilla pattern). It worked well on 2D-3D and 3D-3D matching — settings where the target modality is a point cloud, but broke on 2D-2D. It led us to switch to <a href="https://arxiv.org/abs/2104.09864"><strong>RoPE</strong></a>, which rotates \(Q\) and \(K\) multiplicatively so the inner product itself depends on relative position rather than on a perturbed additive sum, fixed it across the board. The practical lesson, in retrospect, is that for matching you want positional information to inform the <em>geometry</em> of the attention computation, not sit on top of it as an additive perturbation.</p> <p><strong>A decodable positional encoding for the value stream.</strong> There is a parallel choice on the value side. The positional stream is updated as \(P_k \leftarrow A \cdot \mathrm{AbsPE}(X_t) + P_k\), and the final coordinates come out by inverting AbsPE. My first version used a learnable sinusoidal Positional Embedding (PE) — sinusoids passed through an MLP projection — but the decode at the end was an approximation rather than exact, because the MLP non-linearity does not invert cleanly. Once I started thinking of this stream as <em>a value that has to be decodable back to a coordinate</em>, the constraint became obvious: AbsPE has to behave arithmetically like a raw coordinate, so that \(\mathrm{AbsPE}(A) + \mathrm{AbsPE}(B) = \mathrm{AbsPE}(A + B)\). A learned affine map \(W_p X + b_p\) satisfies that exactly, and its Moore–Penrose pseudo-inverse \(W_p^+\) gives a closed-form decode. The deeper point is that linearity is what preserves the global-matching structure end-to-end: the attention-weighted average \(A \cdot \mathrm{AbsPE}(X_t)\) stays equal to AbsPE applied to the regressed target coordinate, instead of drifting through a non-linear distortion.</p> <p><strong>Gaussian attention instead of vanilla dot-product.</strong> This one started somewhere unexpected. Optimal transport (OT) is a long-standing tool for matching and registration — Sinkhorn-style soft matching is widely used across the point-cloud and image-matching literature — and my initial plan was to incorporate an OT-style learnable module into the matching decoder. As I worked through the formulation, the math kept reducing to plain attention with a Gaussian kernel: one entropic-OT iteration with squared-\(L^2\) cost is exactly</p> \[A = \mathrm{Softmax}\left(-\frac{\|F_k' - F_t'\|^2}{D}\right).\] <p>So I kept the form and dropped the framing. In the paper we present it through the cleaner descriptor-matching lens — Gaussian kernels are what classical descriptor matching has always preferred, capturing non-linear similarity and robust to feature magnitude — but the design move was originally derived from OT, and either framing produces the same kernel. Empirically a free win over vanilla dot-product attention, and conceptually it is what the matching framing tells you to use.</p> <p><strong>Single-head attention.</strong> A counterintuitive ablation: since each query has at most one corresponding target token, the matching problem is closer to nearest-neighbor than to multi-relational reasoning. Reducing the number of attention heads from 16 to 1 in the matching decoder consistently improved performance. Multi-head attention in this layer was, in retrospect, an artifact of importing the encoder template uncritically.</p> <h2 id="matching-decoder-layer-and-large-correspondence-model">Matching Decoder Layer and Large Correspondence Model</h2> <p>Once the dual-stream layer is the right shape, two more ingredients turn it from an “interesting decoder block” into “the matching decoder you would want in a large correspondence model.”</p> <p><strong>Auxiliary supervision through the attention itself.</strong> The dual-stream design buys you stackability, but it does not <em>use</em> the stackability for free. The fix came from two threads converging. First, recognizing that attention-as-matching-cost is itself a form of global matching, we ran that observation as a baseline — a single-layer global matcher that reads out coordinates as \(A \cdot X_t\). It works, but only at full feature-map resolution, where the cost is too high to be practical (the “global matching” row in Table 1 of the paper). Second, while the dual-stream decoder with RoPE was already converging well, the intermediate attention maps I inspected were diffuse and not consistently focused on the matching coordinate — the kind of unfocused mid-stack attention Transformer decoders fall into when nothing supervises them. Folding the two together gave the fix: every intermediate layer already produces an attention matrix \(A^{(l)}\) that is, by construction, a soft correspondence map, so apply the same global-matching read-out at each layer as auxiliary supervision. Read out a coordinate estimate \(K_t^{(l)} = A^{(l)} X_t\), supervise it with the same L1 matching loss used at the output, and the diffuse intermediate attention sharpens. The auxiliary loss is</p> \[\mathcal{L}_{\mathrm{aux}} = \sum_{l=1}^{L} \gamma^{L-l} \cdot \frac{1}{N} \sum_{i=1}^{N} \left\| K_t^{(l)}(i) - \bar{K}_t(i) \right\|_1.\] <p>This is the design choice that turns out to have the most empirical weight in the whole decoder, and the one most easily missed. Without it, the per-layer attention heatmaps look essentially random — the layers are ostensibly being trained, but they are not learning to represent anything coherent on their own. With it, every intermediate layer produces a sharp attention map focused on the predicted coordinate, and the layers behave like an honest iterative refinement of the matching estimate. The supplementary visualization makes this dramatically clear:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/geo-matching/aux_heatmap_compare-480.webp 480w,/assets/img/blog/geo-matching/aux_heatmap_compare-800.webp 800w,/assets/img/blog/geo-matching/aux_heatmap_compare-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/geo-matching/aux_heatmap_compare.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Per-layer attention heatmaps without (left) and with (right) auxiliary supervision, green markers indicate the model's predicted coordinates. Without it, intermediate layers are diffuse and noisy. With it, each layer attends coherently to the predicted correspondence. </div> <p>The numbers tell the same story. With a single matching decoder layer, adding the auxiliary loss takes AUC@5° on MegaDepth-1500 from 28.8 to 47.7; with five layers, it still adds 2 points on top of an already strong setup. The implication, which I think is the more important lesson, is that the dual-stream design <em>needs</em> layer-wise supervision to actually behave like a multi-layer refiner, rather than an opaque end-to-end function whose intermediate layers do whatever.</p> <p><strong>A contrastive descriptor loss.</strong> On top of the matching loss, we apply an InfoNCE term between the source and target feature descriptors at ground-truth correspondence locations, following <a href="https://arxiv.org/abs/2406.09756">MASt3R</a>’s descriptor-supervision recipe. This sharpens the underlying feature similarity that the attention mechanism feeds on. Not a novel piece in this paper, but the descriptor and the matching mechanism are entangled, and supervising them jointly works better than supervising matching alone.</p> <p><strong>Putting it together: the Large Correspondence Model.</strong> Stack 8 dual-stream decoder layers on top of a shared cross-attention encoder, sit that on top of modality-specific backbones (a ViT for images, <a href="https://arxiv.org/abs/2312.10035">Point Transformer v3</a> for point clouds), and you have UniCorrn — 600M parameters, one set of decoder weights, three correspondence tasks. Trained jointly on a mix of 2D-2D image pairs, 2D-3D and 3D-3D real correspondence data, and pseudo-3D data lifted from depth maps, the model:</p> <ul> <li>matches the leading 2D-2D specialists on MegaDepth-1500 and ScanNet-1500;</li> <li>beats the previous SOTA 2D-3D specialist by <strong>8% in registration recall</strong> on 7Scenes;</li> <li>beats the previous SOTA 3D-3D specialist by <strong>10% in registration recall</strong> on the harder 3DLoMatch benchmark;</li> <li>and does <strong>zero-shot optical flow on Sintel at 5.2 EPE</strong>, despite never being trained on dynamic or stylized imagery. That is the kind of out-of-distribution generalization signal you would expect from a foundation model — and it is what makes me believe this decoder design is the one that scales. The single unified model also uses 3.5× less memory than the combined memory footprint of separate specialists for the three tasks, which matters quite a bit in deployment.</li> </ul> <p>There is one honest negative result worth surfacing too: joint training on all three tasks does not always beat joint training on two. A gradient-conflict analysis traced this to the normalization layers, which struggle to share statistics across the very different distributions of 2D image features and 3D point features. This is a real bottleneck and a clear next step for anyone trying to push this paradigm further.</p> <h2 id="conclusion">Conclusion</h2> <p>There are a lot of moving parts in UniCorrn, but the thesis underneath is small. A unified, scalable correspondence model needs three things: stackable layers, modality-agnostic structure, and an explicit geometric matching mechanism. The paradigms that currently dominate each give up at least one of these.<strong>Attention-as-matching-cost</strong> gives you all three for free, <em>if</em> you are willing to do the architectural work of (a) decoupling appearance and position into two streams so layers can stack, (b) supervising every layer through its own attention to make those stacks behave, and (c) regressing actual coordinates instead of just descriptors so the output is geometric rather than featural.</p> <p>I think this is the underrated piece. Foundation work on correspondence has been pouring its budget into encoders — bigger backbones, richer feature fusion, more pretraining — and treating the matching decoder as plumbing. But the decoder is where the inductive bias for matching actually lives, and the shape of that decoder determines what the model can do. Cost volumes are pinned to the grid. Nearest neighbor is a single shot. Direct regression is brittle without a similarity prior. A decoder that uses attention as a stackable, iteratively refined matching cost is, in my view, the one with the right architectural <em>shape</em> for the unified correspondence problem — and if a Large Correspondence Model that handles every flavor of correspondence (geometric, semantic, temporal) under a single set of weights does emerge, this is the kind of decoder I would expect to be at its core.</p> <p>What this work demonstrates and what it merely suggests are different things, and I want to be honest about which is which. UniCorrn establishes that this decoder design is <em>better</em> than the alternatives at 600M parameters across three correspondence tasks. It does not yet establish that the design <em>scales</em> in the strong sense the foundation-model framing implies. The decoder-depth ablation plateaus around three layers in the small-scale ablation, and the large-scale model only goes to eight; there is no scaling curve past that yet. Joint training surfaces real gradient conflicts in the normalization layers as the modality count grows, and that does not get easier as semantic and temporal modalities are added. Pseudo-3D data carries a lot of weight in the current results — without it, performance on 7Scenes drops from 77.8 to 15.4 RR. None of these are dealbreakers, but they are the open questions the next round of work has to answer, and I think they are more interesting than the claim of state-of-the-art numbers in a single paper.</p> <p>What I am betting on — and what this post is trying to articulate rather than prove — is the broader instinct underneath the work, the one I keep returning to in everything I do: scaling carries you a long way, and the rest of the way is reached by the right explicit structural inductive bias. Geometry, compositionality, the math of the problem itself. Attention-as-matching-cost is the bias that fits correspondence; what I think is genuinely interesting is finding the others, for the parts of 3D and 4D vision that scaling alone has not yet reached, and figuring out what makes a correspondence model <em>genuinely</em> scalable rather than just bigger.</p>]]></content><author><name></name></author><category term="reflection"/><category term="computer-vision"/><category term="algorithm"/><summary type="html"><![CDATA[On attention-as-matching-cost and a path to scaling correspondence]]></summary></entry><entry><title type="html">Why Cost Volume Construction Can Be a Non-Trivial Yet Interesting Problem in Transformer-Based Models?</title><link href="https://jerrygcding.github.io//blog/2024/cost-vol-meets-trx/" rel="alternate" type="text/html" title="Why Cost Volume Construction Can Be a Non-Trivial Yet Interesting Problem in Transformer-Based Models?"/><published>2024-10-13T20:03:00+00:00</published><updated>2024-10-13T20:03:00+00:00</updated><id>https://jerrygcding.github.io//blog/2024/cost-vol-meets-trx</id><content type="html" xml:base="https://jerrygcding.github.io//blog/2024/cost-vol-meets-trx/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Since the introduction of the Transformer architecture in 2017 by Vaswani et al., it has gained increasing attention from its original domain Natural Language Processing (NLP) to Computer Vision (CV). Because of its impressive learning capability, Transformer-based methods have achieved state-of-the-art (SOTA) results in various tasks. For many research endeavors nowadays, the first thing on the list is to find a way to tokenize structured data representation into unstructured ones that Transformer can process since it only inherently takes a sequence of tokens. This blog won’t go into much detail about the tokenization process as it’s still an open question about what’s the most effective way to preprocess and tokenize a certain data representation, from my perspective. I’d like to talk about in my recent research work <a href="https://jerrygcding.github.io/odtformer/"><em>ODTFormer</em></a>, how we formulate the problem of 3D computer vision, specifically depth estimation and reconstruction, into Transformer-acceptable form with an intuitive yet effective design.</p> <h2 id="cost-volume">Cost Volume</h2> <p>Cost volume in computer vision refers to the volumetric representation of cost or similarity of matching pixel windows or features between two or more images. Why is it essential? Because for stereo (<em>two images or views</em>) and multi-view stereo (<em>more than two views</em>) depth estimation, in the simplest case with two rectified images (<em>in a nutshell, the corresponding points on two images of a 3D point should have the same y-coordinate meaning no vertical shift</em>), the epipolar geometry tells us that the depth (<em>Z</em>) of a certain 3D point, visible on both images, can be calculated as \(Z = B * f / disparity\) as shown in the figure below, where \(B\) refers to the baseline (<em>distance between two cameras</em>), \(f\) is the camera focal length as part of the camera intrinsic and \(disparity\) is the shift in the x-coordinate of the corresponding points’ coordinates on the two images.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/cost-volume/1GFLM-480.webp 480w,/assets/img/blog/cost-volume/1GFLM-800.webp 800w,/assets/img/blog/cost-volume/1GFLM-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/cost-volume/1GFLM.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Image cited from Stanford CS231A Computer Vision lecture. </div> <p>So theoretically, if we can get accurate disparity measurements, depth estimation can be straightforward. But in real-world applications, several detrimental factors exist, like imperfect camera parameters, rectification or lens distortions, etc., one dominant issue is that finding accurate pixel correspondences between the two images can be challenging!</p> <p>Then comes the technique used by most of the depth estimation methods nowadays.</p> <h2 id="learning-based-cost-volume-construction">Learning-Based Cost Volume Construction</h2> <p>Let’s simplify the problem to only about stereo reconstruction that only have two images of different views. Conventionally, the cost volume is constructed by first extracting feature maps using image backbone and then given a predefined depth range, a disparity range of interest can be calculated through reversing the equation above. At each disparity level, feature maps can be overlapped or interlaced on each other, then processed by 2D or 3D convolution blocks to get the matching costs where the values will form distribution to the cost/similarity of certain pixel under this disparity level. Eventually, the matching costs from all disparity levels will be concatenated to form the cost volume (<em>if the size of feature map is</em> \(H \times W\)<em>, it becomes</em> \(D \times H \times W\) <em>where D is the number of disparity levels</em>) and can be processed by another 3D convolution block to get the final volume.</p> <p>Reflecting on this process, it’s an exhaustive search method that iterates through all possible disparities where some camera parameters and training data-specific settings are inevitably being hardcoded into the model during the training process, leading to issues for model generalizability and computation efficiency. But because of the nature of convolution architecture being good at handling structured data representations, such approach shows impressive performances in depth estimation and reconstruction.</p> <h2 id="cost-volume-meets-transformer">Cost Volume meets Transformer</h2> <p>However, the cost volume construction method has some fatal weaknesses when trying to combine it with a Transformer-based architecture. First of all, although there’s already a quite mature pipeline of how Transformer can be used to process 2D image data pioneered by <a href="https://bibbase.org/service/mendeley/bfbbf840-4c42-3914-a463-19024f50b30c/file/264ac473-27b7-bd53-3963-f6a07df9b72e/Dosovitskiy_et_al___2021___An_Image_is_Worth_16x16_Words_Transformers_for_Im.pdf.pdf">Vision Transformer</a> (ViT) through splitting an image into uniform-sized patches and extracting token features for each patch. But the cost volume requires adding dimension to the data which needs to consider a lot more extra factors, such as, what’s the meaning of information carried by the dimension, do we process it as a whole or partition it into more fine-grained features. Moreover, because of the quadratic growth in the computational complexity of the Attention mechanism - the core of Transformer architecture - the effect of resultant tokens from tokenizing the entire 3D volume to the performance of Attention operation cannot be ignored.</p> <p><strong>Is it simply infeasible to adapt the concept of cost volume into Transformer-based models?</strong></p> <h2 id="voxel-occupancy-detection">Voxel Occupancy Detection</h2> <p>Voxel occupancy detection is a subtask of reconstruction in 3D computer vision, the term <strong>Occupancy Grid</strong> is defined as a data structure that partition a 3D space into uniform sized voxel cubes and each cube can be marked as either occupied or unoccupied. The occupancy grid is a common data structure being leveraged in robotic navigation and autonomous driving systems to improve computation efficiency and data interpretability. Previous navigation works using voxel occupancy detection usually lie in two settings - Stereo (<em>two font cameras</em>) and Bird’s-Eye View (BEV, <em>six surrounding cameras</em>).</p> <p>Almost all prior works under the stereo setting use the cost volume construction method since it’s the most straightforward application as we discussed before, the images can be easily be rectified because of the nature of stereo camera setup and it has been proven effective by numerous stereo depth estimation works. What about BEV settings?</p> <p>Adapting cost volume construction to occupancy detection in BEV turns out to be non-trivial given the fact that there are certainly more than horizontal shifts in some camera pairs under BEV settings and how to effectively construct cost volume with more than two views remains an open question under many aspects. Therefore, most of the BEV detection works use Transformer-based methods to tackle this problem. One of the most influential work in BEV is <a href="https://krmzyc-filecloud.oss-cn-beijing.aliyuncs.com/theory/BEVFormer%20Learning%20Bird%27s-Eye-View%20Representation%20from%20Multi-Camera%20Images%20via%20Spatiotemporal%20Trans.pdf">BEVFormer</a> uses a type of spatial attention called Multiscale Deformable Attention mechanism.</p> <h2 id="multiscale-deformable-attention-mda-mechanism">Multiscale Deformable Attention (MDA) Mechanism</h2> <p>The Multiscale Deformable Attention (MDA) mechanism is first introduced by Zhu et al. in <a href="https://arxiv.org/pdf/2010.04159">Deformable DETR</a> for object detection in 2D images. It consists of five steps: first initialize queries from spatial locations on the 2D image, then the queries will be fed to a Learnable Offset Sampler which generates a set of predefined number of offsets for each query per feature map scale. For each query, the offsets will be applied to its associated spatial location on each feature map, producing the set of sampling locations. Finally, the set of feature tokens can be obtained from the feature maps through grid sampling using the sampling locations and the query will be attending the set of selected tokens.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/cost-volume/mda-480.webp 480w,/assets/img/blog/cost-volume/mda-800.webp 800w,/assets/img/blog/cost-volume/mda-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/cost-volume/mda.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Image cited from Deformable DETR: Deformable Transformers for End-to-End Object Detection. </div> <p>The Multiscale Deformable Attention mechanism excels at both task performance and computational efficiency because it’s a type of guided Attention, instead of vanilla Attention which simply attends to all feature tokens, the Deformable Attention only attends to tokens that are considered relevant. In Computer Vision tasks, attending to all tokens can inevitably introduce noise and unwanted information from irrelevant tokens. The sampling process also greatly reduces the computational complexity caused by the quadratic growth in the Attention mechanism as mentioned earlier.</p> <h2 id="mda-in-bev-occupancy-detection">MDA in BEV Occupancy Detection</h2> <p>Since it’s not easy to directly adapt cost volume construction into BEV setups given various constraints, this is where MDA comes in handy. BEVFormer introduced a pipeline using MDA which is referenced by lots of BEV works afterward. As the camera intrinsics and poses are known in these tasks, they can be used to project coordinates in 3D space onto 2D images. Therefore, the Spatial Cross-Attention in BEVFormer works as follows - Given the multi-scale feature maps extracted from BEV images, the queries for voxels within the occupancy grid are initialized using the 3D center coordinates of the voxels and the corresponding 2D coordinates for each view are obtained through direct projection using camera parameters, and then the queries go through the same MDA process to produce query outputs per view. Eventually, the multi-view outputs are aggregated through averaging the outputs from views that the 3D query coordinates fall into and producing the final feature of each voxel.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/cost-volume/bevformer-480.webp 480w,/assets/img/blog/cost-volume/bevformer-800.webp 800w,/assets/img/blog/cost-volume/bevformer-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/cost-volume/bevformer.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Image cited from BEVFormer: Learning Bird’s-Eye-View Representation from Multi-Camera Images via Spatiotemporal Transformers. </div> <h2 id="stereo-occupancy-detection-meets-mda">Stereo Occupancy Detection meets MDA</h2> <p>In our recent work ODTFormer, we aim to solve the stereo occupancy detection problem with a Transformer-based architecture. However, after extensive experiments of directly using the MDA following BEVFormer’s manner, the model always fails to outperform the SOTA method <a href="https://ieeexplore.ieee.org/iel7/10160211/10160212/10160924.pdf?casa_token=XQWpTAYppeAAAAAA:_bQOiZxCykgnjhLZ4M-LmKfp_-41JVVMaCAOgfz5vKERx-3tSvZTg1KSMeXJh8OaQgt7dkU">StereoVoxelNet</a>, uses cost volume construction and Convolutions, in stereo occupancy detection by a great margin. Only when we modify MDA to process 3D data and attending to a conventional cost volume shows some improvements in metric measurements while still unable to get close to StereoVoxelNet.</p> <p>These phenomena makes us realize that matching cost could be an essential inductive bias in 3D reconstruction, but with the obstruction mentioned above, how to effectively combine cost volume construction with Attention or Transformer models?</p> <h2 id="deformable-matching-cost-block">Deformable Matching Cost Block</h2> <p>In ODTFormer, we discovered a simple yet surprisingly effective design that can introduce the matching costs as inductive bias into the Transformer-based model while differing the conventional cost volume construction, called Deformable Matching Cost (DMC) Block.</p> <p>The changes in the DMC operation compared to the usage of DMA in BEVFormer are as follows - instead of carrying out the feature sampling process in 2D space entirely, we use the 3D voxel center coordinates with a set of 3D sampling offsets constrained by the target voxel size to get 3D sampling locations, then locations are projected to 2D corresponding points and get the features. Moreover, to further enforce feature correspondences, we don’t use different sampling locations per scale, but directly scale the sampling coordinates so that they always correspond to the same set of 3D points on different scales. Furthermore, we use two separate Multi-Layer Perceptrons (MLP) to first concatenate and merge the feature channels from the tokens extracted from two views corresponding to the same 3D point per scale, the second MLP is used to concatenate and merge the output tokens across multiple scales to get the final set of tokens that contain multi-view and multi-scale matching cost information.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/cost-volume/3d_sampling-480.webp 480w,/assets/img/blog/cost-volume/3d_sampling-800.webp 800w,/assets/img/blog/cost-volume/3d_sampling-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/cost-volume/3d_sampling.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/cost-volume/multi_scale_sampling-480.webp 480w,/assets/img/blog/cost-volume/multi_scale_sampling-800.webp 800w,/assets/img/blog/cost-volume/multi_scale_sampling-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog/cost-volume/multi_scale_sampling.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Illustration of our approach in 3D-2D correspondence sampling and multi-scale correspondence sampling. </div> <p>Finally, the queries attend to the extracted matching cost tokens to produce the voxel features as a cost volume for each voxels that we are interested in and are further decoded into voxel occupancy grids.</p> <p>The model metric evaluations show that our approach outperforms StereoVoxelNet by a great margin with minimal increase in computational complexity, and although the coarse-grained voxel size we use for sampling is \(3m\), our model still achieves the best accuracy after decoding into fine-grained voxel size of \(0.375m\). This observation further supports that matching cost is an essential factor as inductive bias that can greatly boost reconstruction model performances and the effectiveness of our DMC block design. The best part of it is that the theoretical applications of DMC block can be extended to multi-views with minimal modifications.</p> <h2 id="conclusion">Conclusion</h2> <p>It’s fascinating to dive deep into the topic of how the technique of cost volume construction can be reformulated to fit of a Transformer-based model, but what lies more within such research endeavor is that if we can correctly find the sweet point of how to reformulate the problem representation of the model considering certain invariants. Simple yet intuitive designs can also greatly boost the model performance without affecting computational efficiency which can be useful for improving model robustness and generalizability across different domains and settings, as well as its affordability on edge devices.</p>]]></content><author><name></name></author><category term="reflection"/><category term="computer-vision"/><category term="algorithm"/><summary type="html"><![CDATA[A technical post reflecting our work in leveraging Transformer architecture for reconstruction]]></summary></entry></feed>